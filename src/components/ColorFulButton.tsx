
const ColorFulButton = ({ title }: { title: string }) => {
    return (
        <button className='py-[15px] px-[40px] font-semibold text-[16px] text-white bg-primaryColor'>
            {title}
        </button>
    )
}

export default ColorFulButton
